# URL base de la API de NestJS (incluye el prefijo /api).
VITE_API_URL="http://localhost:3000/api"

# ───────── Clerk (preparado, DESACTIVADO en esta etapa) ─────────
# Cuando se active la auth, completar con la publishable key de Clerk
# y descomentar el ClerkProvider en src/main.tsx.
# VITE_CLERK_PUBLISHABLE_KEY=""
